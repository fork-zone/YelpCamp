<% include partials/header %>

<h1> This is the campgrounds page!</h1>

<% campgrounds.forEach(function(campground){ %>
	<div> 
	<h4><%= campground.name %></h4>
	<img src="<%= campground.image %>" style="width: 300px; height:300px;">
	</div>
	
<%}); %>

<% include partials/footer %>